import utils.*

fun main() {
    Day06(IO.TYPE.SAMPLE).test()
    Day06().solve()
}

class Day06(inputType: IO.TYPE = IO.TYPE.INPUT) : Day("", inputType = inputType) {

    private val data = input.splitLines()
    
    override fun part1(): Any? {
        data
            .print()
        return "not yet implement"
    }

    override fun part2(): Any? {
        return "not yet implement"
    }
}           